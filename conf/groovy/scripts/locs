//Example script to manipulate locations in the map 
//  through binding variable locations


def random_loc1 = locations.get 'ToAvoidLoc1'

def p1 =new Plan(console)

p1.with {
	planName "Location_1"
	goTo location: random_loc1
	addToConsole()
}