ccu = Location.APDL
n = ask 'How many surveys?'
uuvs = pick {
	 count: 2;
	 payload 'Sidescan';
	}

gateway = pick {
	
	payload 'accoustics';
	}

survey1 = imcPlan('survey1') >>  action { post ready:1 }
survey2 = imcPlan('survey2') >>  action { post ready:2 }
//Location 500m south and west from APDL 
wifi_range = ccu.move -500,-500
until (!poll ready:1 & !poll ready:2) {
	skeeping location:wifi_range
	
	}